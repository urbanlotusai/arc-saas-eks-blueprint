formatter: markdown table

output:
  file: README.md
  mode: inject
  template: |-
    <!-- BEGIN_TF_DOCS -->
    {{ .Content }}
    <!-- END_TF_DOCS -->

settings:
  indent: 2
  lockfile: false
  read-comments: true
  required: true
  sensitive: true

sort:
  enabled: true
  by: name
